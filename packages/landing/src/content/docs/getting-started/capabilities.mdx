---
title: Capabilities
description: Built-in capabilities available to Lobu agents.
---

import { CapabilitiesDemoGrid } from "../../../components/CapabilitiesDemoGrid";
import { ToolDemos } from "../../../components/ToolDemos";

export const coreTools = [
  {
    name: "read",
    how: "Reads file content from the current workspace path. It is used before edits to inspect source and config safely.",
    prompt: "Open `src/server.ts` and summarize what it does.",
    response: "Here's src/server.ts — an Express app on port 3000 with 3 routes: /health, /api/users, /api/auth. The auth middleware checks JWT tokens from the Authorization header.",
  },
  {
    name: "write",
    how: "Writes full file content. Best for creating new files or replacing an entire file when structure changes a lot.",
    prompt: "Create `scripts/release.ts` with a release helper.",
    response: "Created scripts/release.ts with version bump, changelog generation, and git tag helpers. Run it with `bun run scripts/release.ts`.",
  },
  {
    name: "edit",
    how: "Applies targeted replacements to existing files. It is optimized for small surgical changes with minimal diff.",
    prompt: "Rename `oldFlag` to `featureFlag` in `src/config.ts` only.",
    response: "Done. Renamed oldFlag → featureFlag in src/config.ts (3 replacements applied).",
  },
  {
    name: "bash",
    how: "Runs shell commands in the agent sandbox under your policy rules. Used for build, test, lint, and automation tasks.",
    prompt: "Run tests for `packages/gateway` and report failing cases.",
    response: "42 passed, 1 failing:\nauth.test.ts — token expiry assertion off by 1s.\n\nWant me to fix the flaky test?",
  },
  {
    name: "grep",
    how: "Searches text patterns in code and logs. It helps the agent quickly locate symbols, errors, and references.",
    prompt: "Find all usages of `sendToWorker` in this repo.",
    response: "Found 7 matches in 4 files:\n• routes/jobs.ts:42,87\n• orchestration/queue.ts:15\n• services/worker.ts:33,58,91\n• tests/worker.test.ts:12",
  },
  {
    name: "find",
    how: "Searches for files and directories by path pattern. Useful in large monorepos before reading files.",
    prompt: "Find all `docker-compose` files in this project.",
    response: "3 compose files found:\n• docker/docker-compose.yml\n• docker/docker-compose.dev.yml\n• docker/docker-compose.test.yml",
  },
  {
    name: "ls",
    how: "Lists directory contents to inspect structure, generated artifacts, and available scripts.",
    prompt: "List files under `packages/worker/src/openclaw`.",
    response: "packages/worker/src/openclaw/\n• processor.ts\n• tools.ts\n• custom-tools.ts\n• worker.ts\n• plugins/",
  },
];

export const customTools = [
  {
    name: "UploadUserFile",
    how: "Sends generated files back to the user chat thread.",
    prompt: "Create a CSV report and upload it.",
    response: "Report generated (24 rows, 3 columns). Uploading churn-report.csv to your thread…\n\nFile uploaded.",
  },
  {
    name: "ScheduleReminder",
    how: "Creates one-time or recurring reminder jobs from natural-language instructions.",
    prompt: "Remind me every Monday at 9am to review PRs.",
    response: "Scheduled: Mon 9 AM — review open PRs\nRecurrence: weekly\nID: sched_a1b2",
  },
  {
    name: "CancelReminder",
    how: "Cancels an existing reminder by schedule ID.",
    prompt: "Cancel reminder `sched_123`.",
    response: "Cancelled reminder sched_123.\nIt was: \"Check deployment status\" — daily at 6 PM.",
  },
  {
    name: "ListReminders",
    how: "Lists active reminders so users can audit or cancel them.",
    prompt: "Show my pending reminders.",
    response: "2 active reminders:\n1. sched_a1b2 — Mon 9 AM: review PRs (weekly)\n2. sched_c3d4 — Tomorrow 2 PM: review Q1 deck (one-off)",
  },
  {
    name: "SearchSkills",
    how: "Searches discoverable skills/MCPs and lists currently installed capabilities.",
    prompt: "Find a skill for on-call incident triage.",
    response: "Found 3 matching skills:\n1. ops-triage — incident triage with PagerDuty + Slack\n2. on-call-helper — on-call runbook automation\n3. alert-summarizer — alert digest and dedup",
  },
  {
    name: "InstallSkill",
    how: "Installs or upgrades skills from the registry, bundling their provider/MCP dependencies into a settings approval flow.",
    prompt: "Install the ops-triage skill.",
    response: "Installing ops-triage…\nNeeds: Gmail (OAuth), GitHub (OAuth), Linear (API key)\n\nOpen settings to connect?",
    buttons: [{ label: "Install Skill", action: "link" }],
  },
  {
    name: "InstallPackage",
    how: "Requests system package installation (nix) with inline user approval buttons.",
    prompt: "I need ffmpeg to process this video.",
    response: "Package install request sent. Click below to approve.",
    buttons: [{ label: "Approve ffmpeg", action: "link" }],
  },
  {
    name: "RequestNetworkAccess",
    how: "Requests access to blocked domains with inline user approval buttons.",
    prompt: "I need to call the GitHub API.",
    response: "Domain access request sent for api.github.com.",
    buttons: [{ label: "Grant Access (1h)", action: "link" }],
  },
  {
    name: "GenerateImage",
    how: "Generates an image from a prompt and sends it back in the chat thread.",
    prompt: "Create a minimalist onboarding illustration with teal accents.",
    response: "Image generated and uploaded to the thread.",
  },
  {
    name: "GenerateAudio",
    how: "Converts text responses into audio output.",
    prompt: "Read this summary aloud and send audio.",
    response: "Audio generated (1:42). Sending voice message…",
  },
  {
    name: "GetChannelHistory",
    how: "Loads earlier thread messages to recover context.",
    prompt: "Review earlier thread context before answering.",
    response: "Loaded 12 earlier messages. Context recovered — you previously asked about deployment timelines and CI config.",
  },
  {
    name: "AskUserQuestion",
    how: "Sends structured button-based prompts for explicit human-in-the-loop decisions.",
    prompt: "Ask user whether to run full migration or dry run.",
    response: "Before proceeding:\n\nRun full migration or dry run first?",
    buttons: [{ label: "Full Migration", action: "link" }, { label: "Dry Run", action: "link" }],
  },
  {
    name: "ConnectService",
    how: "Starts OAuth/API-key connection flow for integrations and MCP servers.",
    prompt: "Connect the user's GitHub account.",
    response: "Starting GitHub OAuth flow. Click below to authorize.",
    buttons: [{ label: "Connect GitHub", action: "link" }],
  },
  {
    name: "CallService",
    how: "Calls external APIs through gateway proxy, where auth and secrets are injected securely.",
    prompt: "List open pull requests from the connected repo.",
    response: "3 open PRs:\n• #142 — fix auth timeout (2 approvals)\n• #138 — add rate limiting (needs review)\n• #135 — update deps (CI failing)",
  },
  {
    name: "DisconnectService",
    how: "Revokes or removes an existing connected integration.",
    prompt: "Disconnect my Linear integration.",
    response: "Linear integration disconnected. Credentials removed.\n\nReconnect anytime via settings.",
  },
];

This page explains how Lobu capabilities run on top of the OpenClaw runtime and Lobu's API runtime.

See also: [Comparison](/getting-started/comparison/), [Skills](/getting-started/skills/), and [Providers](/reference/providers/).

## Runtime architecture

Lobu capability execution has two cooperating runtime layers that split agent logic from platform infrastructure.

### OpenClaw runtime (worker layer)

Each worker runs an [OpenClaw](https://github.com/openclaw/openclaw) session — the open-source agent runtime that handles the core execution loop. OpenClaw manages:

- **Session loop** — receives a prompt, plans tool calls, executes them, and streams results back.
- **Tool execution** — the 7 core tools (`read`, `write`, `edit`, `bash`, `grep`, `find`, `ls`) run directly inside the OpenClaw runtime. The agent decides which tools to call based on the user's request.
- **Plugin hooks** — OpenClaw's plugin system (`pluginsConfig`) lets you swap in memory backends, custom tool sets, and lifecycle hooks without changing Lobu's code.
- **Workspace isolation** — each session operates inside a sandboxed workspace directory. Files persist across turns within the same thread.

Workers never see real credentials. They receive opaque placeholder environment variables; the gateway proxy resolves actual secrets at request time.

### Lobu API runtime (gateway layer)

The gateway sits between users and workers, handling everything outside of agent execution:

- **Platform adapters** — receives messages from Telegram, WhatsApp, Slack, or the REST API and normalizes them into a common job format.
- **Provider proxying** — routes LLM API calls through the gateway proxy, injecting the user's provider credentials per-request. Workers only know placeholder URLs.
- **Auth resolution** — manages OAuth flows, API key storage, and MCP server authentication. Connected services are available to workers via `CallService` without exposing tokens.
- **Scheduling** — `ScheduleReminder` and `CancelReminder` calls are handled by the gateway's job scheduler, which triggers worker sessions at the scheduled time.
- **Policy enforcement** — network allowlists, tool permissions, and package approvals are enforced at the gateway layer before reaching the worker.
- **Custom tools** — the 13 Lobu-specific tools (`UploadUserFile`, `ConnectService`, `AskUserQuestion`, etc.) are API calls from the worker back to the gateway.

This split means agents evolve via config and user approvals — no new integration codepath needed for each provider or skill.

## Interactive capability demos

The demos below use the same settings panels and chat UI components used on the homepage.

<CapabilitiesDemoGrid client:load />

## Core runtime tool overview

From `packages/worker/src/openclaw/tools.ts`. These tools run directly inside the OpenClaw runtime.

| Tool | What it does | Typical use |
|---|---|---|
| `read` | Reads file contents from the workspace. | Inspect source/config/log files before editing. |
| `write` | Writes full file contents. | Create new files or replace file content entirely. |
| `edit` | Applies targeted text replacements in a file. | Small, surgical code changes. |
| `bash` | Runs shell commands in the workspace (policy-controlled). | Build, test, lint, run scripts, inspect runtime state. |
| `grep` | Searches file contents with pattern matching. | Find symbols, config values, or error strings quickly. |
| `find` | Finds files/directories by path patterns. | Locate files across large repositories. |
| `ls` | Lists files/directories. | Quick workspace structure discovery. |

## Lobu custom tool overview

From `packages/worker/src/openclaw/custom-tools.ts`. These tools call back to the Lobu API runtime.

| Tool | What it does | Typical use |
|---|---|---|
| `UploadUserFile` | Uploads a generated file back to the user thread. | Share reports, charts, documents, exports, media. |
| `ScheduleReminder` | Schedules one-time or recurring follow-up tasks. | Deferred tasks and recurring automations. |
| `CancelReminder` | Cancels a scheduled reminder by ID. | Stop previously scheduled jobs. |
| `ListReminders` | Lists pending reminders and schedule IDs. | Audit or pick reminder to cancel/update. |
| `SearchSkills` | Searches for skills and MCP servers, or lists installed capabilities (empty query). | Discover capabilities and check installed state. |
| `InstallSkill` | Installs or upgrades skills from the registry with bundled dependencies. | Add capabilities (skills, providers, MCPs) via settings approval. |
| `InstallPackage` | Requests system package installation (nix) with user approval. | Install ffmpeg, imagemagick, or other system tools. |
| `RequestNetworkAccess` | Requests domain access grants with user approval. | Unblock API endpoints for curl/fetch calls. |
| `GenerateImage` | Creates images from text prompts and uploads them in-thread. | Illustrations, posters, product mockups, and visual concepts. |
| `GenerateAudio` | Converts text to speech and returns audio. | Voice responses or spoken summaries. |
| `GetChannelHistory` | Fetches prior messages in the conversation thread. | Recover context from earlier discussion. |
| `AskUserQuestion` | Sends structured button-based questions to the user. | Branching choices and approvals without free-text ambiguity. |
| `ConnectService` | Authenticates with a service (OAuth integration or MCP server). | Connect user account before API operations. |
| `CallService` | Calls external APIs through gateway-managed auth. | Make authenticated API requests without exposing secrets. |
| `DisconnectService` | Removes service connection/credentials. | Revoke or reset broken/stale integration access. |

## How each tool works

Each section below shows the tool description and an example conversation demonstrating how the agent uses it.

### Core runtime tools (OpenClaw)

These run inside the OpenClaw session. The agent calls them directly without leaving the worker sandbox.

<ToolDemos tools={coreTools} color="#6366f1" client:load />

### Lobu custom tools (API runtime)

These are API calls from the worker to the gateway. Auth, secrets, and platform delivery are handled by the gateway.

<ToolDemos tools={customTools} color="#f59e0b" client:load />

## Additional tool sources

Beyond built-ins, agents can also receive tools from:

- OpenClaw plugins (`pluginsConfig`) loaded at runtime
- Configured MCP servers (proxied through the gateway)

## Memory plugin runtime

Lobu uses OpenClaw's plugin system for memory. The default plugin is Owletto (`@lobu/owletto-openclaw`, slot `memory`).

You can switch to another OpenClaw memory plugin, such as `@openclaw/native-memory`, via `pluginsConfig`.
