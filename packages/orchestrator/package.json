{
  "name": "@peerbot/orchestrator",
  "version": "1.0.0",
  "description": "Simple deployment orchestrator for peerbot worker management",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun src/index.ts",
    "start": "cd ../shared && bun run build && cd ../orchestrator && bun src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@peerbot/shared": "workspace:*",
    "dockerode": "^4.0.7",
    "dotenv": "^17.2.1",
    "pg-boss": "^10.0.4"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.29",
    "@types/pg": "^8.11.6",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
