diff --git a/dist/index.js b/dist/index.js
index 2d48f2187a6f56d896a3459018ea53f637981484..a34bbcaeb2abf207524617b724cc2d898e6730d6 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -38,10 +38,17 @@ function toInlineKeyboardRow(actions) {
       continue;
     }
     if (action.type === "link-button") {
-      row.push({
-        text: convertLabel(action.label),
-        url: action.url
-      });
+      if (action.webApp) {
+        row.push({
+          text: convertLabel(action.label),
+          web_app: { url: action.url }
+        });
+      } else {
+        row.push({
+          text: convertLabel(action.label),
+          url: action.url
+        });
+      }
     }
   }
   return row;
@@ -524,7 +531,7 @@ var TelegramAdapter = class {
     const parsedThread = this.resolveThreadId(threadId);
     const card = extractCard(message);
     const replyMarkup = card ? cardToTelegramInlineKeyboard(card) : void 0;
-    const parseMode = card ? TELEGRAM_MARKDOWN_PARSE_MODE : void 0;
+    const parseMode = card ? TELEGRAM_MARKDOWN_PARSE_MODE : (typeof message === "object" && message !== null && "html" in message ? "HTML" : (typeof message === "object" && message !== null && "markdown" in message ? TELEGRAM_MARKDOWN_PARSE_MODE : void 0));
     const text = this.truncateMessage(
       convertEmojiPlaceholders2(
-        card ? cardToFallbackText(card) : this.formatConverter.renderPostable(message),
+        card ? cardToFallbackText(card) : (typeof message === "object" && message !== null && "html" in message ? message.html : this.formatConverter.renderPostable(message)),
         "gchat"
       )
     );
@@ -590,7 +597,7 @@ var TelegramAdapter = class {
     } = this.decodeCompositeMessageId(messageId, parsedThread.chatId);
     const card = extractCard(message);
     const replyMarkup = card ? cardToTelegramInlineKeyboard(card) : void 0;
-    const parseMode = card ? TELEGRAM_MARKDOWN_PARSE_MODE : void 0;
+    const parseMode = card ? TELEGRAM_MARKDOWN_PARSE_MODE : (typeof message === "object" && message !== null && "html" in message ? "HTML" : (typeof message === "object" && message !== null && "markdown" in message ? TELEGRAM_MARKDOWN_PARSE_MODE : void 0));
     const text = this.truncateMessage(
       convertEmojiPlaceholders2(
-        card ? cardToFallbackText(card) : this.formatConverter.renderPostable(message),
+        card ? cardToFallbackText(card) : (typeof message === "object" && message !== null && "html" in message ? message.html : this.formatConverter.renderPostable(message)),
         "gchat"
       )
     );
